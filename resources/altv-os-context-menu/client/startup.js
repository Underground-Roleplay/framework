import * as alt from 'alt-client';
import * as context from '/client/context';
import * as contextInteractionBuilder from '/client/interactionBuilder';

alt.on('context:Ready', () => {
    alt.emit('context:CreateMenu', 2287735495, 'Gas Station');
    alt.emit(
        'context:AppendToMenu',
        2287735495,
        'buy gas',
        'Station:open',
        false
    );
});

alt.on('context:Ready', () => {
    alt.emit('context:CreateMenu', -2007231801, 'Gas Station');
    alt.emit(
        'context:AppendToMenu',
        -2007231801,
        'buy gas',
        'Station:open',
        false
    );
});

alt.on('context:Ready', () => {
    alt.emit('context:CreateMenu', 1339433404, 'Gas Station');
    alt.emit(
        'context:AppendToMenu',
        1339433404,
        'buy gas',
        'Station:open',
        false
    );
});

alt.on('context:Ready', () => {
    alt.emit('context:CreateMenu', 1694452750, 'Gas Station');
    alt.emit(
        'context:AppendToMenu',
        1694452750,
        'buy gas',
        'Station:open',
        false
    );
});

alt.on('context:Ready', () => {
    alt.emit('context:CreateMenu', 1933174915, 'Gas Station');
    alt.emit(
        'context:AppendToMenu',
        1933174915,
        'buy gas',
        'Station:open',
        false
    );
});

alt.on('context:Ready', () => {
    alt.emit('context:CreateMenu', -462817101, 'Gas Station');
    alt.emit(
        'context:AppendToMenu',
        -462817101,
        'buy gas',
        'Station:open',
        false
    );
});

alt.on('context:Ready', () => {
    alt.emit('context:CreateMenu', -469694731, 'Gas Station');
    alt.emit(
        'context:AppendToMenu',
        -469694731,
        'buy gas',
        'Station:open',
        false
    );
});

alt.on('context:Ready', () => {
    alt.emit('context:CreateMenu', -164877493, 'Gas Station');
    alt.emit(
        'context:AppendToMenu',
        -164877493,
        'buy gas',
        'Station:open',
        false
    );
});

alt.on('context:Ready', () => {
    alt.emit('context:CreateMenu', 4130089803, 'Gas Station');
    alt.emit(
        'context:AppendToMenu',
        4130089803,
        'buy gas',
        'Station:open',
        false
    );
});
alt.on('context:Ready', () => {
    alt.emit('context:CreateMenu', 3832150195, 'Gas Station');
    alt.emit(
        'context:AppendToMenu',
        3832150195,
        'buy gas',
        'Station:open',
        false
    );
});
